[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "./App serve --env production --hostname 0.0.0.0 --port ${PORT:-8080}"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
